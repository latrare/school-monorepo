CREATE TABLE PATIENT (ID BIGINT NOT NULL, NAME VARCHAR(255), PATIENTID BIGINT, PRIMARY KEY (ID))
CREATE TABLE TREATMENT (ID BIGINT NOT NULL, TYPE VARCHAR(31), DIAGNOSIS VARCHAR(255), TREATMENT_PATIENT BIGINT, TREATMENT_PROVIDER BIGINT, DOSAGE FLOAT, DRUG VARCHAR(255), SURGERYDATE DATE, PRIMARY KEY (ID))
CREATE TABLE PROVIDER (ID BIGINT NOT NULL, NAME VARCHAR(255), NPI BIGINT, PROVIDERTYPE INTEGER, PRIMARY KEY (ID))
CREATE TABLE RadiologyTreatment_TREATMENTDATES (RadiologyTreatment_ID BIGINT, TREATMENTDATES DATE)
ALTER TABLE TREATMENT ADD CONSTRAINT FK_TREATMENT_TREATMENT_PROVIDER FOREIGN KEY (TREATMENT_PROVIDER) REFERENCES PROVIDER (ID)
ALTER TABLE TREATMENT ADD CONSTRAINT FK_TREATMENT_TREATMENT_PATIENT FOREIGN KEY (TREATMENT_PATIENT) REFERENCES PATIENT (ID)
ALTER TABLE RadiologyTreatment_TREATMENTDATES ADD CONSTRAINT FK_RadiologyTreatment_TREATMENTDATES_RadiologyTreatment_ID FOREIGN KEY (RadiologyTreatment_ID) REFERENCES TREATMENT (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
